package com.pennywiseai.ynab.ui.rules

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

// TEMPORARY STUB — replaced by the real implementation in Task 9.
// Signature is load-bearing: PennyWiseApp (Task 6) calls it with these exact params.
@Composable
fun RuleEditorScreen(args: com.pennywiseai.ynab.ui.nav.Screen.RuleEditor, onDone: () -> Unit) {
    Text("Rule editor")
}
