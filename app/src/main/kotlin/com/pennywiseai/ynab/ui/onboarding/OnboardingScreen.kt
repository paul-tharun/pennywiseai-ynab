package com.pennywiseai.ynab.ui.onboarding

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

// TEMPORARY STUB — replaced by the real implementation in Task 12.
// Signature is load-bearing: PennyWiseApp (Task 6) calls it with these exact params.
@Composable
fun OnboardingScreen(onRequestPermissions: () -> Unit, onComplete: () -> Unit) {
    Text("Onboarding")
}
