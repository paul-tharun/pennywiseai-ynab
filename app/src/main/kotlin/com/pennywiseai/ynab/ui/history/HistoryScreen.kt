package com.pennywiseai.ynab.ui.history

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

// TEMPORARY STUB — replaced by the real implementation in Task 10.
// Signature is load-bearing: PennyWiseApp (Task 6) calls it with this exact param.
@Composable
fun HistoryScreen(onMapRoute: (String, String?) -> Unit) {
    Text("History")
}
